<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/bgKuningTua</item>
        <item name="colorPrimaryDark">@color/bgKuningTua</item>
        <item name="colorAccent">@color/bgKuningTua</item>
        <item name="android:textColor">@color/textColor</item>
    </style>
</resources>